# 自動OCRリネームエージェント

PDF/画像ファイルをOCRで読み取り、内容に基づいて自動リネームする。
事前定義された処理コース・処理アクションから**各1つずつ**選択し、実行と報告を行う。

**独自の処理モードを発明してはならない。報告を省略してはならない。**

---

## 実行環境

- **プロジェクト**: `fax-ocr-js/`
- **エントリポイント**: `node src/main.js`（単一/バッチ処理）、`node src/watch.js`（監視モード）
- **設定ファイル**: `.env`

---

## A. 処理コース（1つ選択・必須）

以下の3つから**必ず1つ**選択すること。選択したコースとその理由を明示すること。

### 1) BATCH_ALL（全件処理）

| 項目 | 内容 |
|------|------|
| **概要** | 対象フォルダ内の全ファイルを処理する。過去の処理履歴を無視する。 |
| **用途** | 初回一括処理、再処理が必要な場合、処理結果をリセットしたい場合。 |
| **注意点** | `--force` により既存キャッシュを無視するため、処理時間が長くなる。既に処理済みのファイルも再度OCR・リネーム対象となる。 |

**対応コマンド:**

```bash
node src/main.js --in "{入力フォルダ}" --out "{出力フォルダ}" --force --searchable-pdf --apply-rename
```

### 2) BATCH_DIFF（差分処理）

| 項目 | 内容 |
|------|------|
| **概要** | 未処理のファイルのみを処理する。既存のOCR/リネーム結果があるファイルはスキップする。 |
| **用途** | 定期実行、前回処理の続きから再開したい場合。 |
| **注意点** | 既存の `.ocr.json` や `.rename.json` が存在するファイルは自動スキップされる。スキップされたファイルは進捗報告に含めること。 |

**対応コマンド:**

```bash
node src/main.js --in "{入力フォルダ}" --out "{出力フォルダ}" --searchable-pdf --apply-rename
```

### 3) WATCH_NEW（監視モード）

| 項目 | 内容 |
|------|------|
| **概要** | フォルダを継続的に監視し、新規追加ファイルが出現したときのみ処理する。 |
| **用途** | 夜間無人運用、FAX受信フォルダの常時監視。 |
| **注意点** | プロセスが常駐する。`--apply-rename`、`--searchable-pdf`、`--llm-rename` がデフォルトで有効。`--no-scan` を付けると起動時の既存ファイル処理をスキップする。ファイル書き込み安定まで2秒待機してから処理を開始する。 |

**対応コマンド:**

```bash
node src/watch.js "{監視フォルダ}" "{出力フォルダ}"
```

起動時の既存ファイルをスキップする場合:

```bash
node src/watch.js "{監視フォルダ}" "{出力フォルダ}" --no-scan
```

---

## B. 処理アクション（1つ選択・必須）

以下の2つから**必ず1つ**選択すること。選択したアクションとその理由を明示すること。

### 1) OCR_TO_SEARCHABLE_PDF（OCR実行＋検索可能PDF生成＋リネーム）

| 区分 | 内容 |
|------|------|
| **何をするか** | OCRを実行する。透明テキスト付きPDF（searchable PDF）を生成する。OCR結果に基づいてリネームルールを適用する。 |
| **何をしないか** | 既存のOCR結果は再利用しない（`--force` 併用時）。テキスト抽出のみでは終わらず、必ずPDF生成まで行う。 |

**対応コマンド（単一ファイル）:**

```bash
node src/main.js process \
    --in "{入力ファイル}" \
    --out "{出力フォルダ}" \
    --searchable-pdf \
    --font-path "{フォントパス}" \
    --apply-rename
```

### 2) RENAME_ONLY_FROM_SEARCHABLE_PDF（リネームのみ）

| 区分 | 内容 |
|------|------|
| **何をするか** | 既存の検索可能PDFからテキストを参照する。ファイル名のリネームのみを実施する。 |
| **何をしないか** | OCRは実行しない。新たなPDFは生成しない。画像変換は行わない。 |

**対応コマンド（単一ファイル）:**

```bash
node src/main.js process \
    --in "{入力ファイル}" \
    --out "{出力フォルダ}" \
    --skip-ocr \
    --apply-rename
```

---

## C. 必須運用ルール

以下のルールは他の全ての考慮事項に**優先**する。

### 自動承認

- リネーム提案は**常に自動承認**される（ユーザー確認は行わない）
- 提案されたファイル名がそのまま適用される

### 上書きリネーム

- リネームは**既存ファイル名を上書き**する形で行う
- 同名ファイルが既に存在する場合は末尾に連番（`_1`, `_2` ... `_99`）を付与する
- 複製ファイル・コピー・バックアップは**作成しない**

### 自動アクション切替

- ファイルが既に検索可能PDFであり、選択アクションが `OCR_TO_SEARCHABLE_PDF` の場合:
  - 自動的に `RENAME_ONLY_FROM_SEARCHABLE_PDF` に**切り替える**
  - 切り替えが発生した場合は**報告対象**とする（セクションG・Hに記載）
- 選択したコース/アクションで処理できないファイルは**スキップ**し、理由を報告する

---

## D. コマンドパラメータ一覧

| パラメータ | 説明 |
|-----------|------|
| `--in` | 入力ファイル/フォルダ（PDF/画像） |
| `--out` | 出力フォルダ |
| `--searchable-pdf` | 透明テキスト付きPDF生成 |
| `--font-path` | 日本語フォント（searchable PDF用） |
| `--skip-ocr` | 既存.ocr.jsonを再利用（OCRスキップ） |
| `--dpi` | PDF変換DPI（デフォルト: 300） |
| `--force` | キャッシュを無視して再OCR |
| `--apply-rename` | リネーム提案を実際に適用 |
| `--gemini` | Gemini APIでMarkdown整形 |
| `--llm-refine` | ローカルLLMでテキスト補正 |
| `--llm-refine-mode` | 補正モード: `text`（デフォルト）/ `lines`（bbox保持） |
| `--llm-rename` | 2モデル推論でリネーム精度向上（Gemma3 → Qwen3） |
| `--no-scan` | Watchモード起動時の既存ファイル処理をスキップ |

---

## E. ファイル名ルール（デフォルト）

### 一般文書

```
{日付}_{種別}_{会社名}_{内容}.pdf
```

| 項目 | ルール | 例 |
|------|--------|-----|
| 日付 | `YYYYMMDD`（ハイフンなし） | `20260127` |
| 種別 | 件名から取得 | `見積依頼`, `注文書`, `納品書`, `請求書`, `領収書` |
| 会社名 | **送信先**（依頼主）を採用 | `三八商店` |
| 内容 | 材質_点数 | `SS400_3点` |

### ミルシート用

```
{日付}_{材質}_{板厚}_{メーカー}_{チャージNo}.pdf
```

| 項目 | ルール | 例 |
|------|--------|-----|
| メーカー | 製鉄メーカーを抽出（商社は除外） | `東京製鉄`, `中山製鋼所` |
| チャージNo | メーカー固有のパターンで抽出 | `AB12345` |
| 板厚 | 寸法パターンから抽出 | `9t` |

### 例

```
元: 2026-01-27_FAX_富陽金属株式会社.pdf
後: 20260127_見積依頼_三八商店_SS400_3点.pdf
```

### 信頼度スコア

| 抽出シグナル数 | 信頼度 |
|--------------|--------|
| 4以上 | 0.95 |
| 3 | 0.90 |
| 2 | 0.70 |
| 1 | 0.40 |
| 0 | 0.10（元のファイル名を維持） |

---

## F. 自社名除外ルール

**重要**: `日興金属` は自社名なので会社名として**採用しない**。

```
NG: 20260127_見積依頼_日興金属_SS400.pdf
OK: 20260127_見積依頼_三八商店_SS400.pdf
```

自社名が検出された場合は、次に優先度の高い会社名（送信元など）を採用する。

---

## G. 進捗報告（必須）

処理の実行中に以下の情報を**逐次報告**すること。

| 報告項目 | 内容 |
|---------|------|
| 処理コース | 選択した処理コース（BATCH_ALL / BATCH_DIFF / WATCH_NEW） |
| 処理アクション | 選択した処理アクション（OCR_TO_SEARCHABLE_PDF / RENAME_ONLY_FROM_SEARCHABLE_PDF） |
| 検出ファイル総数 | フォルダ内の対象ファイル数 |
| 処理済み件数 | 現時点で処理が完了したファイル数 |
| スキップ件数 | スキップしたファイル数と理由（差分処理で既存結果あり、処理不可、等） |
| 自動切替 | アクションの自動切替が発生した場合、そのファイル名と理由 |

**報告フォーマット:**

```
### 開始報告
処理コース: <BATCH_ALL | BATCH_DIFF | WATCH_NEW>
処理アクション: <OCR_TO_SEARCHABLE_PDF | RENAME_ONLY_FROM_SEARCHABLE_PDF>
検出ファイル総数: N

[処理ログ]
- [1/N] ファイル名 → 処理中...
- [1/N] ファイル名 → 完了（リネーム: 新ファイル名）
- [2/N] ファイル名 → スキップ（理由: ...）
- [3/N] ファイル名 → アクション自動切替: RENAME_ONLY_FROM_SEARCHABLE_PDF（理由: 既に検索可能PDF）
```

---

## H. 最終結果報告（必須）

全ファイルの処理完了後に、以下のサマリーを**必ず出力**すること。

```
### 最終報告
処理コース: <BATCH_ALL | BATCH_DIFF | WATCH_NEW>
処理アクション: <OCR_TO_SEARCHABLE_PDF | RENAME_ONLY_FROM_SEARCHABLE_PDF>
ファイル総数: N
処理済み: N
スキップ: N
  - 理由1: N件
  - 理由2: N件
OCR実行: N
リネーム実行: N
自動切替発生: あり / なし
  - 対象ファイル: （切替が発生した場合のみ列挙）
備考:
- （処理中に発生した特記事項）
```

---

## I. 出力ファイル

| ファイル | 説明 |
|----------|------|
| `*.ocr.json` | OCR結果（bbox付き） |
| `*.rename.json` | リネーム提案 |
| `*.gemini.md` | Gemini整形Markdown |
| `*.searchable.pdf` | 透明テキスト付きPDF |

---

## J. 注意事項・前提条件

- `.env` に `OCR_PROVIDER` 設定が必要（デフォルト: `local`）
- ローカルOCR使用時は Docker で PaddleOCR を起動: `docker-compose up -d`
- `--gemini` 使用時は `GEMINI_API_KEY` が必要
- `--llm-refine` / `--llm-rename` 使用時は `LLM_ENABLED=true` と LM Studio 起動が必要
- Python依存: `pip install -r py/requirements.txt`
- 日本語フォント指定で searchable PDF 生成
- ファイル名は最大200文字に切り詰められる
- Windows予約名（CON, PRN, AUX, NUL, LPT/COM）は自動回避される
