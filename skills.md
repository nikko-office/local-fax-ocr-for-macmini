# 汎用OCRリネーマー

PDFや画像をOCRで読み取り、内容に基づいてファイル名を提案・リネームする。

---

## 実行環境

- **プロジェクト**: `fax-ocr-js/`
- **エントリポイント**: `node src/main.js`
- **設定ファイル**: `.env`

---

## コマンド

### 単一ファイル処理

```bash
node src/main.js process \
    --in "{入力ファイル}" \
    --out "{出力フォルダ}" \
    --gemini \
    --searchable-pdf \
    --font-path "{フォントパス}"
```

### バッチ処理

```bash
node src/main.js --in "{入力フォルダ}" --out "{出力フォルダ}"
```

### パラメータ

| パラメータ | 説明 |
|-----------|------|
| `--in` | 入力ファイル/フォルダ（PDF/画像） |
| `--out` | 出力フォルダ |
| `--gemini` | Gemini APIでMarkdown整形 |
| `--searchable-pdf` | 透明テキスト付きPDF生成 |
| `--font-path` | 日本語フォント（searchable PDF用） |
| `--skip-ocr` | 既存.ocr.jsonを再利用 |
| `--dpi` | PDF変換DPI (default: 300) |
| `--force` | キャッシュを無視して再OCR |
| `--apply-rename` | リネーム提案を実際に適用 |

---

## 処理手順

1. **入力確認**
   - 指定フォルダ内のPDF/画像ファイル一覧を取得
   - 件数をユーザーに報告

2. **OCR実行（1ファイルずつ）**
   - `node src/main.js process` を実行
   - 出力フォルダに OCR結果・searchable PDF を保存

3. **結果表示・ファイル名提案**
   - OCRで読み取った内容を表示
   - 内容に基づいてファイル名を提案
   - 例: `20260129_見積依頼_三八商店_SS400_3点.pdf`

4. **ユーザー確認**
   - 提案ファイル名でOKか確認
   - 修正指示があれば反映

5. **リネーム実行**
   - 確定したファイル名でリネーム
   - 次のファイルへ

6. **ルール学習（オプション）**
   - 「同じルールで残り全部やって」→ 一括処理
   - 修正パターンを記憶して適用

---

## 出力

| ファイル | 説明 |
|----------|------|
| `*.ocr.json` | OCR結果（bbox付き） |
| `*.rename.json` | リネーム提案 |
| `*.gemini.md` | Gemini整形Markdown |
| `*.searchable.pdf` | 透明テキスト付きPDF |

---

## 使用例

### 基本

```
「このフォルダのPDFをOCRしてリネームして」
→ フォルダパスを指定
→ 1件ずつ確認しながら処理
```

### 一括処理

```
「このフォルダ全部、内容見てファイル名つけて」
→ 最初の数件を確認
→ 「このルールで全部やって」
→ 残り一括処理
```

### ファイル名ルール指定

```
「会社名_材質_板厚の形式でリネームして」
→ 指定フォーマットで提案
```

---

## ファイル名ルール（デフォルト）

```
{日付}_{種別}_{会社名}_{内容}.pdf
```

| 項目 | ルール | 例 |
|------|--------|-----|
| 日付 | `YYYYMMDD`（ハイフンなし） | `20260127` |
| 種別 | 件名から取得 | `見積依頼`, `注文書`, `納品書` |
| 会社名 | **送信先**（依頼主）を採用 | `三八商店` |
| 内容 | 材質_点数 | `SS400_3点` |

### 例

```
元: 2026-01-27_FAX_富陽金属株式会社.pdf
後: 20260127_見積依頼_三八商店_SS400_3点.pdf
```

---

## 自社名除外ルール

**重要**: `日興金属` は自社名なので会社名として採用しない

```
❌ 20260127_見積依頼_日興金属_SS400.pdf
✅ 20260127_見積依頼_三八商店_SS400.pdf
```

---

## 注意事項

- 1ファイルずつ処理（process コマンド）またはバッチ処理可能
- `.env` に `GOOGLE_CLOUD_API_KEY` 設定が必要
- `--gemini` 使用時は `GEMINI_API_KEY` も必要
- Python依存: `pip install -r py/requirements.txt`
- 日本語フォント指定でsearchable PDF生成
